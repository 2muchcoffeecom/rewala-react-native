import { StyleSheet } from 'react-native';
import { backgroundColor } from '../../../../../../app.style';

const style = StyleSheet.create({
  root: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingTop: 57,
    backgroundColor,
  },
});

export default style;