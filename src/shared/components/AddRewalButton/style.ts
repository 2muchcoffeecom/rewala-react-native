import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  image: {
    width: 50,
    height: 50,
  },
  addButton: {
    position: 'absolute',
    bottom: 26,
    right: 24,
    width: 50,
    height: 50,
    borderRadius: 4,
  },
});

export default style;