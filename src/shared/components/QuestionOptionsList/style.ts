import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  root: {
    width: '100%',
    marginBottom: 12,
    paddingLeft: 18,
    paddingRight: 18,
  },
});

export default style;